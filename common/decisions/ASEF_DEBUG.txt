DEBUG_FOCUS_decisions = {

	LOAD_PRC_00 = {
		icon = GFX_win98_thumbnail
		allowed = {
			original_tag = PRC
        }
		visible = {
			original_tag = PRC
			is_debug = yes
		}
		available = {
			original_tag = PRC
			is_debug = yes
		}
		cost = 0
		fire_only_once = no
		complete_effect = {
			load_focus_tree = {
                tree = PRC_00
                keep_completed = yes
			}
			mark_focus_tree_layout_dirty = yes
		}
	}
	
	LOAD_PRC_01 = {
		icon = GFX_win98_thumbnail
		allowed = {
			original_tag = PRC
        }
		visible = {
			original_tag = PRC
			is_debug = yes
		}
		available = {
			original_tag = PRC
			is_debug = yes
		}
		cost = 0
		fire_only_once = no
		complete_effect = {
			load_focus_tree = {
                tree = PRC_01
                keep_completed = yes
			}
			mark_focus_tree_layout_dirty = yes
		}
	}

	LOAD_PRC_01_FS_TYR = {
		icon = GFX_win98_thumbnail
		allowed = {
			original_tag = PRC
        }
		visible = {
			original_tag = PRC
			is_debug = yes
		}
		available = {
			original_tag = PRC
			is_debug = yes
		}
		cost = 0
		fire_only_once = no
		complete_effect = {
			mark_focus_tree_layout_dirty = yes
			if = {
				limit = { has_country_flag = PRC_01_FS_TYR_ALLOW }
				clr_country_flag = PRC_01_FS_TYR_ALLOW 
			}
			else = {
				set_country_flag = PRC_01_FS_TYR_ALLOW
			}		
		}
	}
}